query greWords($where: greWordsBoolExp, $skip: Int, $take: Int) {
  greWords(where: $where, skip: $skip, take: $take) {
    id
    spelling
    gptPrompts {
      id
      input
      response
    }
  }
}
